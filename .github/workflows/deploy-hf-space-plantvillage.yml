name: Deploy HF Space - PlantVillage CLIP HyCoCLIP

on:
  push:
    branches: [main]
    paths:
      - spaces/plantvillage-clip-hycoclip/**
      - .github/workflows/deploy-hf-space-plantvillage.yml
      - .github/workflows/deploy-hf-space-reusable.yml
  repository_dispatch:
    types: [pypi-release]
  workflow_dispatch:

concurrency:
  group: deploy-hf-space-plantvillage
  cancel-in-progress: false

jobs:
  deploy:
    uses: ./.github/workflows/deploy-hf-space-reusable.yml
    with:
      source_dir: spaces/plantvillage-clip-hycoclip
      space_id: maximilian-franz/HyperView
    secrets: inherit
